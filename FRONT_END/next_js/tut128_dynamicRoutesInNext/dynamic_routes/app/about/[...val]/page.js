export default function Page({ params }) {
    console.log(params)
    // throw new Error("error xa")

    // fetch your blog post by its slug...
    
    return <div>Its about page check console..</div>
  }
